# Local container connections
local   all   all                     trust

# Trust Adminer/Docker network only
host    all   all   172.20.0.101/32     trust

# Everything else requires password
host    all   all   0.0.0.0/0         scram-sha-256
